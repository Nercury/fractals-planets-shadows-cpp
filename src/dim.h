#pragma once

/*#include "types.h"

class Vector2f
{
public:
	float x;
	float y;
	Vector2f() : x(0), y(0) {};
	Vector2f(float x, float y) : x(x), y(y) { };
};

class Rotation3f
{
public:
	float yaw; // z -180, 180 
	float pitch; // y -90, 90 
	float roll; // x -180, 180
	Rotation3f() : yaw(0), pitch(0), roll(0) {};
	Rotation3f(float yaw, float pitch, float roll) : yaw(yaw), pitch(pitch), roll(roll) {};
};

class Vector3f
{
public:
	float x;
	float y;
	float z;
	Vector3f() : x(0), y(0), z(0) {};
	Vector3f(float x, float y, float z) : x(x), y(y), z(z) { };
	Vector3f(Rotation3f rotation, float length) {
		
	};
};*/